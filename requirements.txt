# --- Core app (required to just run app.py with the free Gemini API) ---
gradio>=4.44,<7
python-dotenv>=1.0.0
google-generativeai>=0.7.0

# --- Only needed if you run the local LoRA/QLoRA fine-tuning steps ---
# (scripts/finetune_lora.py, scripts/compare_models.py, local model backend)
# Install these separately on a GPU machine (e.g. Google Colab):
#
# torch>=2.1.0
# transformers>=4.44.0
# datasets>=2.20.0
# peft>=0.12.0
# trl>=1.10.0
# accelerate>=0.33.0
# bitsandbytes>=0.43.0   # only needed for QLoRA (CUDA GPU required)
